services:
  hydro:
    build: .
    ports:
      - "3000:3000"
    restart: unless-stopped
    volumes:
      - hydro-data:/app/data
    environment:
      - HYDRO_TRACE=0
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  hydro-data:
